# Python files
*.py[cod]
*.py.swo
*.egg-info
__pycache__
build
dist

# Virtual environments (never commit these)
venv/
venv1/
.venv/
env/
ENV/

# Make files
Makefile

# Build files
.coverage
coverage.xml
htmlcov

# Editor files
## Vim files
.*.swp
.idea

# Environment files with secrets
.env
.env.local
.env.production
.env.*.local

# Keep example files
!.env.example

# Secrets and keys
*.key
*.pem
*.crt
*.cer
secrets/

# Prometheus data and local configs with secrets
prometheus-data/
prometheus.yml

# Database backups (contain sensitive data and are too large for git)
backups/
backup.sh

# Log files (runtime data, can be large, may contain sensitive info)
logs/*.log

# NextReel Requirements — Production
# ===================================
# Last updated: 2026-03-22
#
# Core Web Framework & Server
# ---------------------------
quart==0.20.0                    # Async web framework (Flask-like API)
hypercorn==0.17.3                # ASGI server for running Quart apps
quart-session==3.0.0             # Session management for Quart
werkzeug==3.1.6                  # WSGI utilities (used by Quart)

# Database & Storage
# -----------------
aiomysql==0.3.2                  # Async MySQL/MariaDB client (pulls PyMySQL transitively)
redis>=4.2.0,<6                  # redis-py; <6 required by arq 0.26.x (server can be Redis 6/7)

# API Clients & HTTP
# -----------------
httpx==0.28.1                    # Modern async HTTP client (used for TMDb, PostHog reverse proxy)
requests>=2.32,<3                # Sync HTTP — directly imported by logging_config.LokiHandler; also a transitive dep of posthog. Pinned explicitly so a fresh prod install never lacks it.

# Security & Authentication
# ------------------------
cryptography==46.0.5             # Cryptographic primitives for security features
authlib>=1.3.0                   # OAuth 2.0 client for Google/Apple Sign-In
bcrypt>=4.0.0                    # Password hashing
email-validator>=2.0.0           # Email format validation

# Configuration & Environment
# --------------------------
python-dotenv==1.0.1             # Load environment variables from .env file

# Monitoring & Observability
# -------------------------
prometheus-client==0.21.1        # Prometheus metrics collection
posthog>=3.7.0,<4                # Product analytics — server-side event capture, identify, feature flags. Non-blocking by design.

# Logging & Debugging (Optional but Recommended)
# ----------------------------------------------
colorlog==6.9.0                  # Colored terminal output for logs

# Task Scheduling & Background Jobs
# ---------------------------------
arq==0.26.3                      # Redis-backed worker for enrichment and refresh jobs

# Utilities
# --------
jinja2==3.1.6                    # Template engine (used by Quart)
markupsafe==3.0.2                # Safe string markup for Jinja2
